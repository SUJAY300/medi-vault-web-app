# MySQL (structured user data) - use with docker-compose
DATABASE_URL="mysql://medivault:medivault@localhost:3306/medivault"

# MongoDB (unstructured documents) - use with docker-compose
MONGODB_URI="mongodb://localhost:27017/medivault"

# Twilio (for OTP SMS delivery)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
